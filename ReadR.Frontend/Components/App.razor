<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <link rel="stylesheet" href="@Assets["lib/bootstrap/dist/css/bootstrap.min.css"]" />
    <link rel="stylesheet" href="@Assets["app.css"]" />
    <link rel="stylesheet" href="@Assets["ReadR.Frontend.styles.css"]" />
    <ImportMap />
    <link rel="icon" type="image/png" href="favicon.png" />
    <HeadOutlet />
    <style>
        .hover-bg-light:hover {
            background-color: #f8f9fa;
            transition: background-color 0.2s ease;
        }

        .card {
            box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);
            border: 1px solid rgba(0, 0, 0, 0.125);
            height: 100%; /* Ensure cards fill their container height */
        }   

        .badge {
            font-size: 0.7em;
        }

        .feed-card {
            transition: transform 0.2s ease, box-shadow 0.2s ease;
            border: 1px solid rgba(0, 0, 0, 0.125);
            height: 100%; /* Ensure feed cards fill container height */
            display: flex; /* Make card a flex container */
            flex-direction: column; /* Stack content vertically */
        }

        .feed-card .card-body {
            display: flex;
            flex-direction: column;
            flex: 1; /* Allow card body to grow and fill available space */
            min-height: 0; /* Allow flex item to shrink below content size */
        }

        .feed-card .card-text {
            flex-grow: 1; /* Allow description to grow and fill available space */
        }

            .feed-card:hover {
                transform: translateY(-2px);
                box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15) !important;
            }

        .badge {
            font-size: 0.65em;
        }

        .card-title a:hover {
            color: var(--bs-primary) !important;
        }

        /* Ensure all columns in a row have equal height */
        .row.g-4 > [class*="col-"] {
            display: flex;
            flex-direction: column;
        }

        /* Additional utility class for equal height cards */
        .equal-height-cards .card {
            height: 100%;
            display: flex;
            flex-direction: column;
        }

        .equal-height-cards .card-body {
            flex: 1;
            display: flex;
            flex-direction: column;
        }
    </style>
</head>

<body>
    <Routes />
    <script src="_framework/blazor.web.js"></script>
</body>

</html>
